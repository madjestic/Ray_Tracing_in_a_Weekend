# build instructions
```bash
  $ cd ${project_dir}
  $ make 
```
# set up RTags:
```bash
  $ cd ${project_dir} 
  $ rdm &                     # start a rtags daemon
  $ rc -J .                   # index the project with rtags
```
